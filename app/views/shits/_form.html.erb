<%= form_for(@shit) do |f| %>
  <% if @shit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shit.errors.count, "error") %> prohibited this shit from being saved:</h2>

      <ul>
      <% @shit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
